---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Form from '../components/Form.astro'
import CardImage from '../components/CardImage.astro'
import CardIcon from '../components/CardIcon.astro'
import CardBlog from '../components/CardBlog.astro'
import Footer from '../components/Footer.astro'

import { getFeatures, getPortfolio, getSocialNetworks, getHomePosts } from '../lib/datocms.js';

const { allFeatures } = await getFeatures();
const { allPortfolios } = await getPortfolio();
const { allSocialNetworks } = await getSocialNetworks();
const { allBlogs } = await getHomePosts();
---

<Layout title="emamut">
  <div class="row mt-5">
    <div class="col-md-6">
      <h1 class="fw-bold">
        Soluciones <br>
        <span class="text-orange">Tecnológicas</span> <br>
        Innovadoras
      </h1>
      <p class="mt-3">
        En <span class="fw-bold text-orange">emamut</span> transformamos ideas en realidades digitales.
        Desarrollamos software a medida, aplicaciones web y móviles que
        impulsan el crecimiento de tu negocio.
      </p>
    </div>
    <div class="col-md-6">
      <img src="https://placehold.co/600x400" alt="" class="img-fluid">
    </div>
  </div>
</Layout>
<style>
  h1 {
    font-size: 60px;
    color: #111827;
  }
</style>